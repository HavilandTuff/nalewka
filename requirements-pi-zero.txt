# Requirements optimized for Raspberry Pi Zero deployment
# This file contains a minimal set of dependencies for running on resource-constrained devices

# Core Flask framework
flask==3.1.2
flask-login==0.6.3
flask-sqlalchemy==3.1.1
flask-wtf==1.2.2
flask-migrate==4.1.0

# Database - Using SQLite for simplicity on Pi Zero
# Note: psycopg2-binary is excluded as it's not needed for SQLite
sqlalchemy==2.0.43
alembic==1.16.5

# Web server - Using built-in Flask server instead of Gunicorn for Pi Zero
# gunicorn is excluded as it's too heavy for Pi Zero
werkzeug==3.1.3

# Form handling and validation
wtforms==3.2.1
email-validator==2.3.0

# Configuration management
pydantic==2.11.7
pydantic-settings==2.10.1
python-dotenv==1.1.1

# Authentication
pyjwt==2.10.1

# Template engine
jinja2==3.1.6

# Utility libraries
click==8.1.8
blinker==1.9.0
typing-extensions==4.15.0
idna==3.10
dnspython==2.7.0

# Note: For Pi Zero, we're using SQLite instead of PostgreSQL
# If you need PostgreSQL, you would need to install psycopg2 from source
# which is more complex on ARM devices
